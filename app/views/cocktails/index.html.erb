    <h1>Awesome list of cocktails</h1>
    <h2><%= link_to "Add a cocktail", new_cocktail_path %></h2>
    <br>


    <br>

    <% @cocktails.each do |cocktail| %>
          <h2><%= link_to cocktail.name, cocktail_path(cocktail) %>
              <%= cl_image_tag cocktail.photo, height: 100, width: 100, crop: :fill %>
          </h2>
    <% end %>


      </div>
